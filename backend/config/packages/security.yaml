security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'
    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email
    firewalls:
        login:
            pattern: ^/api/auth
            stateless: true
            # json_login:
            #     check_path: /api/auth
            #     username_path: email
            #     success_handler: lexik_jwt_authentication.handler.authentication_success
            #     failure_handler: lexik_jwt_authentication.handler.authentication_failure
        api:
            pattern: ^/api
            stateless: true
            jwt: ~
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            lazy: true
            provider: app_user_provider
            custom_authenticator: App\Security\AdminAuthenticator
            logout:
                path: app_logout
                target: app_login
                # where to redirect after logout
                # target: app_any_route

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#the-firewall

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        # Ads
        - { path: ^/api/ads, methods: [GET], roles: [PUBLIC_ACCESS] }
        - { path: ^/api/ad/\d+, methods: [GET], roles: [PUBLIC_ACCESS] }
        - { path: ^/api/ad/\d+, methods: [PUT, DELETE], roles: [ROLE_SERVICE_PROVIDER] }
        - { path: ^/api/ad, methods: [POST], roles: [ROLE_SERVICE_PROVIDER] }

        # Users (and their datas)
        - { path: ^/api/user/\d+/ads, methods: [GET], roles: [ROLE_SERVICE_PROVIDER] }
        - { path: ^/api/user/\d+/approaches, methods: [GET], roles: [ROLE_SERVICE_PROVIDER] }
        - { path: ^/api/user/\d+/events, methods: [GET], roles: [ROLE_INDIVIDUAL] }
        - { path: ^/api/user/, methods: [POST], roles: [PUBLIC_ACCESS] }
        - { path: ^/api/user/\d+, methods: [GET], roles: [PUBLIC_ACCESS] }
        - { path: ^/api/user/\d+, methods: [PUT, DELETE], roles: [ROLE_INDIVIDUAL] }

        # Events
        - { path: ^/api/event, methods: [POST], roles: [ROLE_INDIVIDUAL] }
        - { path: ^/api/event/\d+, methods: [PUT, DELETE], roles: [ROLE_INDIVIDUAL] }
        
        # Approaches
        - { path: ^/api/approach, methods: [POST], roles: [ROLE_INDIVIDUAL] }
        - { path: ^/api/approach/\d+, methods: [PUT], roles: [ROLE_INDIVIDUAL, ROLE_SERVICE_PROVIDER] }

when@test:
    security:
        password_hashers:
            # By default, password hashers are resource intensive and take time. This is
            # important to generate secure password hashes. In tests however, secure hashes
            # are not important, waste resources and increase test times. The following
            # reduces the work factor to the lowest possible values.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
