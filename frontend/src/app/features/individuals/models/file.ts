/** User file (profile picture or banner). */
export class File {
  
  /** File ID. */
  public id = NaN;

  /** File name. */
  public fileName = '';

  /** File path. */
  public filePath = '';

  /** File type (profile_picture or banner). */
  public fileType = '';

}